name: limitando triggers
  on:
    push:
      branches:
        - main
        - develop

jobs:
  ci-cd:
    name: Teste de Integração
    runs-on: ubuntu-latest
    steps:
      -run: echo "Ola, Mundo!"
